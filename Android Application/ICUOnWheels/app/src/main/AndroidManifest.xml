<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hplaptop.icuonwheels">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ICU On Wheels"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Navigator"
            android:label="ICU On Wheels"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Location" />
        <activity android:name=".Cancellation" />
        <activity android:name=".Facility" />
        <activity android:name=".RequestFacility" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Patient" />
        <activity android:name=".ForgetPassword" />
        <activity android:name=".Reset_Pwd" />
        <activity android:name=".Hospital_List" />
        <activity android:name=".Booking" />
        <activity
            android:name=".Splash"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BookingList" />
        <activity android:name=".View_Profile" />
        <activity android:name=".Edit_Profile" />
        <activity android:name=".Change_Pwd" />
        <activity android:name=".Registration" />
        <activity android:name=".Clinic_List" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".MapsActivity" />
        <activity android:name=".Add_facility" />
        <activity
            android:name=".InternetCheck"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Nurse"></activity>
    </application>

</manifest>